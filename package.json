{
  "private": true,
  "scripts": {
    "start": "concurrently \"lerna run --parallel start\" \"yarn serve\"",
    "build": "lerna run build",
    "serve": "lerna run --parallel serve",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "@bluebeam/prettier-config-atlas": "^0.5.0",
    "concurrently": "5.3.0",
    "jest": "^26.5.3",
    "lerna": "3.22.1",
    "prettier": "2.1.2"
  }
}
